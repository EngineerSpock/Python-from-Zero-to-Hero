1) Является ли хорошей практикой использование global?

- Да, никаких проблем
+ Нет, модификация глобальных атрибутов это нехорошо

2) Есть две функции: func1 и func2. func1 определяет переменную x, а затем определяет внутри себя func2.
 
Область видимости переменной x для func2 это:

- local
- global
+ enclosing local
- built-in

3) Декораторы позволяют:

+ Модифицировать поведение функции без изменения её кода
- Модифицировать поведение функции, изменяя её код

4) Встроенная функция, которая позволяет "склеивать" коллекции это:

- all
+ zip
- combine

5) Верно ли, что zip работает посредством lazy evaluation?

+ Да
- Нет

6) zip возвращает:

- сразу результат
+ специальный итерируемый объект

7) Верно ли, что лямбды позволяют избегать написания однострочных-функций, использующихся только однажды?

+ Верно
- Не верно

8) Может ли лямбда содержать более одного выражения (statement)?

- Может
+ Не может